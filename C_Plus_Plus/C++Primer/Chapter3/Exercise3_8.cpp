#include "INCLUDE.hpp"

int main()
{
    string str;
    cin >> str;

/*
    decltype(str.size()) count = str.size();
    while(count > 0)
    {
        str[count-1] = 'X';
        count--;
    }
*/
/*
    for(decltype(str.size()) i = 0; i < str.size(); i++)
        str[i] = 'X';
    
    cout << str << endl;
*/
    return 0;
}